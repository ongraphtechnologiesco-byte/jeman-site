{
  "name": "jeman-payments-server",
  "version": "1.0.0",
  "description": "Backend for Jeman Collections — handles KCB Buni STK Push + IPN so credentials never touch the browser.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}

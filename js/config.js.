/* ============================================================
   JEMAN — site config
   ------------------------------------------------------------
   Leave API_BASE_URL empty ("") to run everything in demo mode
   (local simulation, nothing leaves the browser).

   Once server/ is deployed, set this to its public URL, e.g.
     window.JEMAN_API_BASE_URL = "https://jeman-payments.onrender.com";
   and the storefront, account page, admin panel, and order
   tracking all switch to talking to the real backend — no other
   code changes needed.
   ============================================================ */
window.JEMAN_API_BASE_URL = "";
